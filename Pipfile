[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pre-commit = "*"

[packages]
sphinx = ">=4.3, <4.4"
sphinxcontrib-plantuml = ">=0.23, <0.24"
Pillow = ">=7.0.0, <7.1.0"
kconfiglib = ">=14.1, <15.0"
# urllib3>=2 requires openssl>=1.1.1 which is not available on CentOS 7 CI nodes
urllib3 = "<2"

[requires]
python_version = "3.8"
